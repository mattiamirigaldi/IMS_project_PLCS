import 'package:flutter/material.dart';

class FeedDashBoard extends StatelessWidget {
  const FeedDashBoard({Key? key}) : super(key: key);
  @override 
  Widget build(BuildContext context){
    return Column(
      children: const <Widget>[
      Text("FeedNews")
    ]);
  }
}